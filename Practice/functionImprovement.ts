/*
-A function called calculateArea that takes two parameters weight and height.
and returns the area as a number.
-Make second paramter optional if not provided, function should return the area
of a square: width * width
-Add default values to both parameters so function returns 1 if no arguments
are provided.

*/

function calculateArea(width: number, height?: number){
    //define our function, assign types to our parameters.
    //adding a '?' makes our parameter optional


}